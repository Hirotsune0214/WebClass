/*
function sortedList(array) {

  let temp = array[0]; //1
  for(let i = 0; i < array.length; i++){
    if(temp <= numList[i + 1]){
      return true
  } else {
      return false
  }   
} //for
  return temp
} //function

//let numList = [1,2,3,4,5,6]; // True
let numList = [ 50, 10, 4, 4 , 2, 3] // False

let sortedArray = sortedList(numList);
console.log(sortedArray);
*/

/*function ascnedingSortedList(array){
  //let j; 23行目に定義しなくても25行目で定義すれば良いのでは??
  for(let i = 0; i < numList.length; i++){
    let j = i + 1;
    if(array[i] > array[j] ){
      return false;
    }
  }
  return true;
} //function

let numList = [ 50, 10, 4, 4 , 2, 3] // False
//let numList = [1,2,3,4,5,6]; // True
console.log(ascnedingSortedList(numList));*/